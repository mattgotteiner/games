## 1. Repository and Toolchain

- [x] 1.1 Initialize the existing directory as a `main`-branch Git repository,
  add the minimal public-repository metadata, and scaffold strict Preact,
  TypeScript, Vite, and component-test tooling without replacing OpenSpec files.

## 2. Mobile Shell

- [x] 2.1 Implement and test the `/games/`-based responsive application identity,
  typed empty catalog, "No games yet" state, phone layouts, safe-area spacing,
  semantic structure, and visible focus styling.

## 3. Validation and Automation

- [x] 3.1 Add locked validation and GitHub Actions workflows that type-check,
  test, build, and deploy validated `main` artifacts to GitHub Pages.

## 4. Publication

- [x] 4.1 Run the local checks, create and push the public
  `mattgotteiner/games` repository, enable Pages through GitHub Actions, and
  verify the deployed shell at desktop and phone viewport sizes.
