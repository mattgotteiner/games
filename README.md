# Games

A mobile-friendly game catalog built with Preact, TypeScript, and Vite.

## Development

```sh
npm install
npm run dev
```

Use `npm run check` to run the type check, component tests, and production
build.

